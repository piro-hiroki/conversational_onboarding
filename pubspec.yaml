name: conversational_onboarding
description: Declarative, conversational onboarding flows for Flutter. Build Cal AI / Duolingo-style onboarding with branching, animations, and a typed answers map.
version: 0.1.0
repository: https://github.com/piro-hiroki/conversational_onboarding
issue_tracker: https://github.com/piro-hiroki/conversational_onboarding/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
